// Copyright 2026, Jamf Software LLC
// Code generated by jamf-cli generator. DO NOT EDIT.
package generated

import (
	"bytes"
	"encoding/json"
	"fmt"
	"io"
	"os"
	"strings"

	"github.com/Jamf-Concepts/jamf-cli/internal/registry"
	"github.com/spf13/cobra"
)

// NewAppRequestCmd creates the app-request command group
func NewAppRequestCmd(ctx *registry.CLIContext) *cobra.Command {
	cmd := &cobra.Command{
		Use:         "app-request",
		Short:       "Manage app-request",
		Long:        `Manage app-request in Jamf Pro.`,
		Annotations: map[string]string{"jamf:api": "pro"},
	}

	cmd.AddCommand(newAppRequestGetCmd(ctx))
	cmd.AddCommand(newAppRequestUpdateCmd(ctx))

	return cmd
}

func newAppRequestGetCmd(ctx *registry.CLIContext) *cobra.Command {
	var ()

	cmd := &cobra.Command{
		Use:   "get",
		Short: "Get Applicastion Request Settings",
		Long:  "Get app request settings",
		Example: `  # Get app-request
  jamf-cli pro app-request get

  # Get app-request and output as YAML
  jamf-cli pro app-request get -o yaml`,
		Annotations: map[string]string{"jamf:privileges": "Read App Request Settings", "jamf:api": "pro", "jamf:gateway-privileges": "app-request:read"},
		RunE: func(cmd *cobra.Command, args []string) error {
			reqCtx := cmd.Context()

			// Build request path
			path := "/v1/app-request/settings"

			// Build query string
			var queryParts []string
			if len(queryParts) > 0 {
				path = path + "?" + strings.Join(queryParts, "&")
			}

			// Make request
			resp, err := ctx.Client.Do(reqCtx, "GET", path, nil)
			if err != nil {
				return err
			}
			defer resp.Body.Close()

			return ctx.Output.PrintResponse(resp)
		},
	}

	return cmd
}

func newAppRequestUpdateCmd(ctx *registry.CLIContext) *cobra.Command {
	var (
		flagScaffold bool
		flagSet      []string
	)

	cmd := &cobra.Command{
		Use:   "update",
		Short: "Update Application Request Settings",
		Long:  "Update app request settings\n\nUse --set KEY=VALUE to update individual fields (repeatable). The current resource is fetched, your changes are merged in, read-only fields are dropped, and the whole record is written back. Omitted fields keep their current values.\n\nAvailable fields:\n  appStoreLocale                               string\n  isEnabled                                    boolean\n  requesterUserGroupId                         integer\n\nArray and object fields accept a JSON value (e.g. --set field='[\"a\",\"b\"]'):\n  approverEmails                               array\n\nWithout --set, pipe a full JSON document to stdin to replace the resource entirely.",
		Example: `  # Update individual fields (fetch-merge-replace)
  jamf-cli pro app-request update --set field=value

  # Replace app-request from a full JSON document
  jamf-cli pro app-request get -o json | jq '.field = "value"' | jamf-cli pro app-request update

  # Update from a file
  jamf-cli pro app-request update --from-file app-request.json`,
		Annotations: map[string]string{"jamf:privileges": "Update App Request Settings", "jamf:api": "pro", "jamf:gateway-privileges": "app-request:update"},
		RunE: func(cmd *cobra.Command, args []string) error {
			reqCtx := cmd.Context()

			if flagScaffold {
				return printScaffoldOutput(`{
  "appStoreLocale": "deviceLocale",
  "approverEmails": [],
  "isEnabled": true,
  "requesterUserGroupId": 1
}`, ctx.Output.Format())
			}

			// Build request path
			path := "/v1/app-request/settings"

			// Build query string
			var queryParts []string
			if len(queryParts) > 0 {
				path = path + "?" + strings.Join(queryParts, "&")
			}

			// Make request
			// Read body from stdin if available
			var body io.Reader
			var normalized []byte
			if len(flagSet) > 0 {
				// --set: fetch current state, drop read-only / server-computed fields,
				// merge the caller's changes, and PUT the full record back. Fields not
				// named in --set keep their current values.
				existing, ferr := fetchForMerge(reqCtx, ctx.Client, path)
				if ferr != nil {
					return ferr
				}
				current := map[string]any{}
				if len(existing) > 0 {
					if err := json.Unmarshal(existing, &current); err != nil {
						return fmt.Errorf("parsing current app-request for --set: %w", err)
					}
				}
				(&fieldFilter{fields: map[string]*fieldFilter{"appStoreLocale": nil, "approverEmails": nil, "isEnabled": nil, "requesterUserGroupId": nil}}).apply(current)
				setDoc, serr := buildMergePatchFromSet(flagSet, map[string]string{"appStoreLocale": "string", "approverEmails": "array", "isEnabled": "boolean", "requesterUserGroupId": "integer"})
				if serr != nil {
					return serr
				}
				setMap := map[string]any{}
				if err := json.Unmarshal(setDoc, &setMap); err != nil {
					return err
				}
				deepMergeJSON(current, setMap)
				merged, merr := json.Marshal(current)
				if merr != nil {
					return merr
				}
				normalized = merged
				if setStat, _ := os.Stdin.Stat(); setStat != nil && (setStat.Mode()&os.ModeCharDevice) == 0 {
					fmt.Fprintln(os.Stderr, "warning: --set and piped stdin are mutually exclusive; ignoring stdin")
				}
			}
			stat, _ := os.Stdin.Stat()
			if len(flagSet) == 0 && (stat.Mode()&os.ModeCharDevice) == 0 {
				raw, err := io.ReadAll(io.LimitReader(os.Stdin, 10<<20))
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				normalized, err = normalizeInputToJSON(raw)
				if err != nil {
					return err
				}
			}
			if len(normalized) > 0 {
				body = bytes.NewReader(normalized)
			}
			resp, err := ctx.Client.Do(reqCtx, "PUT", path, body)
			if err != nil {
				return err
			}
			defer resp.Body.Close()

			return ctx.Output.PrintResponse(resp)
		},
	}

	cmd.Flags().BoolVar(&flagScaffold, "scaffold", false, "Print a JSON template for the request body and exit")
	cmd.Flags().StringArrayVar(&flagSet, "set", nil, "Update a field via fetch-merge-replace (key=value in dot notation, repeatable)")
	_ = cmd.RegisterFlagCompletionFunc("set", func(_ *cobra.Command, _ []string, _ string) ([]string, cobra.ShellCompDirective) {
		return []string{
			"appStoreLocale=", "isEnabled=", "requesterUserGroupId=",
		}, cobra.ShellCompDirectiveNoSpace
	})
	return cmd
}
