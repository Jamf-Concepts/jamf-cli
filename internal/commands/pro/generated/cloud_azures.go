// Copyright 2026, Jamf Software LLC
// Code generated by jamf-cli generator. DO NOT EDIT.
package generated

import (
	"bytes"
	"encoding/json"
	"fmt"
	"io"
	"net/http"
	"net/url"
	"os"
	"strings"

	"github.com/Jamf-Concepts/jamf-cli/internal/cooldown"
	"github.com/Jamf-Concepts/jamf-cli/internal/registry"
	"github.com/spf13/cobra"
)

// NewCloudAzuresCmd creates the cloud-azures command group
func NewCloudAzuresCmd(ctx *registry.CLIContext) *cobra.Command {
	cmd := &cobra.Command{
		Use:         "cloud-azures",
		Short:       "Manage cloud-azures",
		Long:        `Manage cloud-azures in Jamf Pro.`,
		Annotations: map[string]string{"jamf:api": "pro"},
	}

	cmd.AddCommand(newCloudAzuresGetCmd(ctx))
	cmd.AddCommand(newCloudAzuresCreateCmd(ctx))
	cmd.AddCommand(newCloudAzuresUpdateCmd(ctx))
	cmd.AddCommand(newCloudAzuresDeleteCmd(ctx))

	return cmd
}

func newCloudAzuresGetCmd(ctx *registry.CLIContext) *cobra.Command {
	var ()

	cmd := &cobra.Command{
		Use:   "get <id>",
		Short: "Get Azure Cloud Identity Provider configuration with given ID.",
		Long:  "Get Azure Cloud Identity Provider configuration with given ID.",
		Example: `  # Get a cloud-azure by ID
  jamf-cli pro cloud-azures get 1

  # Get a cloud-azure and output as YAML
  jamf-cli pro cloud-azures get 1 -o yaml`,
		Annotations: map[string]string{"jamf:privileges": "Read LDAP Servers", "jamf:api": "pro"},
		Args:        cobra.ExactArgs(1),
		RunE: func(cmd *cobra.Command, args []string) error {
			reqCtx := cmd.Context()

			// Build request path
			path := "/v1/cloud-azure/{id}"
			path = strings.Replace(path, "{id}", url.PathEscape(args[0]), 1)

			// Build query string
			var queryParts []string
			if len(queryParts) > 0 {
				path = path + "?" + strings.Join(queryParts, "&")
			}

			// Make request
			resp, err := ctx.Client.Do(reqCtx, "GET", path, nil)
			if err != nil {
				return err
			}
			defer resp.Body.Close()

			return ctx.Output.PrintResponse(resp)
		},
	}

	return cmd
}

func newCloudAzuresCreateCmd(ctx *registry.CLIContext) *cobra.Command {
	var (
		flagScaffold bool
	)

	cmd := &cobra.Command{
		Use:   "create",
		Short: "Create Azure Cloud Identity Provider configuration",
		Long:  "Create new Azure Cloud Identity Provider configuration with unique display name.",
		Example: `  # Show the JSON template for creating a cloud-azure
  jamf-cli pro cloud-azures create --scaffold

  # Create a cloud-azure from JSON
  echo '{"name":"Example"}' | jamf-cli pro cloud-azures create

  # Get a cloud-azure, modify it, and create a copy
  jamf-cli pro cloud-azures get 1 -o json | jq '.name = "Copy"' | jamf-cli pro cloud-azures create`,
		Annotations: map[string]string{"jamf:privileges": "Create LDAP Servers", "jamf:api": "pro"},
		RunE: func(cmd *cobra.Command, args []string) error {
			reqCtx := cmd.Context()

			if flagScaffold {
				return printScaffoldOutput(`{
  "cloudIdPCommon": {},
  "server": {}
}`, ctx.Output.Format())
			}

			// Build request path
			path := "/v1/cloud-azure"

			// Build query string
			var queryParts []string
			if len(queryParts) > 0 {
				path = path + "?" + strings.Join(queryParts, "&")
			}

			// Make request
			// Read body from stdin if available
			var body io.Reader
			var normalized []byte
			stat, _ := os.Stdin.Stat()
			if (stat.Mode() & os.ModeCharDevice) == 0 {
				raw, err := io.ReadAll(io.LimitReader(os.Stdin, 10<<20))
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				normalized, err = normalizeInputToJSON(raw)
				if err != nil {
					return err
				}
			}
			if len(normalized) > 0 {
				body = bytes.NewReader(normalized)
			}
			resp, err := ctx.Client.Do(reqCtx, "POST", path, body)
			if err != nil {
				return err
			}
			defer resp.Body.Close()

			return ctx.Output.PrintResponse(resp)
		},
	}

	cmd.Flags().BoolVar(&flagScaffold, "scaffold", false, "Print a JSON template for the request body and exit")
	return cmd
}

func newCloudAzuresUpdateCmd(ctx *registry.CLIContext) *cobra.Command {
	var (
		flagScaffold bool
		flagSet      []string
	)

	cmd := &cobra.Command{
		Use:   "update <id>",
		Short: "Update Azure Cloud Identity Provider configuration",
		Long:  "Update Azure Cloud Identity Provider configuration. Cannot be used for partial updates, all content body must be sent.\n\nUse --set KEY=VALUE to update individual fields (repeatable). The current resource is fetched, your changes are merged in, read-only fields are dropped, and the whole record is written back. Omitted fields keep their current values.\n\nAvailable fields:\n  cloudIdPCommon.displayName                   string\n  cloudIdPCommon.id                            string\n  cloudIdPCommon.providerName                  string\n  server.enabled                               boolean\n  server.id                                    string\n  server.membershipCalculationOptimizationEnabled boolean\n  server.searchTimeout                         integer\n  server.transitiveDirectoryMembershipEnabled  boolean\n  server.transitiveMembershipEnabled           boolean\n  server.transitiveMembershipUserField         string\n\nArray and object fields accept a JSON value (e.g. --set field='[\"a\",\"b\"]'):\n  cloudIdPCommon                               object\n  server                                       object\n  server.mappings                              object\n\nWithout --set, pipe a full JSON document to stdin to replace the resource entirely.",
		Example: `  # Update individual fields (fetch-merge-replace)
  jamf-cli pro cloud-azures update 1 --set field=value

  # Replace a cloud-azure from JSON
  echo '{"name":"Updated"}' | jamf-cli pro cloud-azures update 1

  # Get a cloud-azure, modify, and update
  jamf-cli pro cloud-azures get 1 -o json | jq '.name = "New Name"' | jamf-cli pro cloud-azures update 1`,
		Annotations: map[string]string{"jamf:privileges": "Update LDAP Servers", "jamf:api": "pro"},
		Args:        cobra.ExactArgs(1),
		RunE: func(cmd *cobra.Command, args []string) error {
			reqCtx := cmd.Context()

			if flagScaffold {
				return printScaffoldOutput(`{
  "cloudIdPCommon": {},
  "server": {}
}`, ctx.Output.Format())
			}

			// Build request path
			path := "/v1/cloud-azure/{id}"
			path = strings.Replace(path, "{id}", url.PathEscape(args[0]), 1)

			// Build query string
			var queryParts []string
			if len(queryParts) > 0 {
				path = path + "?" + strings.Join(queryParts, "&")
			}

			// Make request
			// Read body from stdin if available
			var body io.Reader
			var normalized []byte
			if len(flagSet) > 0 {
				// --set: fetch current state, drop read-only / server-computed fields,
				// merge the caller's changes, and PUT the full record back. Fields not
				// named in --set keep their current values.
				existing, ferr := fetchForMerge(reqCtx, ctx.Client, path)
				if ferr != nil {
					return ferr
				}
				current := map[string]any{}
				if len(existing) > 0 {
					if err := json.Unmarshal(existing, &current); err != nil {
						return fmt.Errorf("parsing current cloud-azure for --set: %w", err)
					}
				}
				(&fieldFilter{fields: map[string]*fieldFilter{"cloudIdPCommon": &fieldFilter{fields: map[string]*fieldFilter{"displayName": nil, "id": nil, "providerName": nil}}, "server": &fieldFilter{fields: map[string]*fieldFilter{"enabled": nil, "id": nil, "mappings": &fieldFilter{fields: map[string]*fieldFilter{"building": nil, "department": nil, "email": nil, "groupId": nil, "groupName": nil, "phone": nil, "position": nil, "realName": nil, "room": nil, "userId": nil, "userName": nil}}, "membershipCalculationOptimizationEnabled": nil, "searchTimeout": nil, "transitiveDirectoryMembershipEnabled": nil, "transitiveMembershipEnabled": nil, "transitiveMembershipUserField": nil}}}}).apply(current)
				setDoc, serr := buildMergePatchFromSet(flagSet, map[string]string{"cloudIdPCommon": "object", "cloudIdPCommon.displayName": "string", "cloudIdPCommon.id": "string", "cloudIdPCommon.providerName": "string", "server": "object", "server.enabled": "boolean", "server.id": "string", "server.mappings": "object", "server.membershipCalculationOptimizationEnabled": "boolean", "server.searchTimeout": "integer", "server.transitiveDirectoryMembershipEnabled": "boolean", "server.transitiveMembershipEnabled": "boolean", "server.transitiveMembershipUserField": "string"})
				if serr != nil {
					return serr
				}
				setMap := map[string]any{}
				if err := json.Unmarshal(setDoc, &setMap); err != nil {
					return err
				}
				deepMergeJSON(current, setMap)
				merged, merr := json.Marshal(current)
				if merr != nil {
					return merr
				}
				normalized = merged
				if setStat, _ := os.Stdin.Stat(); setStat != nil && (setStat.Mode()&os.ModeCharDevice) == 0 {
					fmt.Fprintln(os.Stderr, "warning: --set and piped stdin are mutually exclusive; ignoring stdin")
				}
			}
			stat, _ := os.Stdin.Stat()
			if len(flagSet) == 0 && (stat.Mode()&os.ModeCharDevice) == 0 {
				raw, err := io.ReadAll(io.LimitReader(os.Stdin, 10<<20))
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				normalized, err = normalizeInputToJSON(raw)
				if err != nil {
					return err
				}
			}
			if len(normalized) > 0 {
				body = bytes.NewReader(normalized)
			}
			resp, err := ctx.Client.Do(reqCtx, "PUT", path, body)
			if err != nil {
				return err
			}
			defer resp.Body.Close()

			return ctx.Output.PrintResponse(resp)
		},
	}

	cmd.Flags().BoolVar(&flagScaffold, "scaffold", false, "Print a JSON template for the request body and exit")
	cmd.Flags().StringArrayVar(&flagSet, "set", nil, "Update a field via fetch-merge-replace (key=value in dot notation, repeatable)")
	_ = cmd.RegisterFlagCompletionFunc("set", func(_ *cobra.Command, _ []string, _ string) ([]string, cobra.ShellCompDirective) {
		return []string{
			"cloudIdPCommon.displayName=", "cloudIdPCommon.id=", "cloudIdPCommon.providerName=", "server.enabled=", "server.id=", "server.membershipCalculationOptimizationEnabled=", "server.searchTimeout=", "server.transitiveDirectoryMembershipEnabled=", "server.transitiveMembershipEnabled=", "server.transitiveMembershipUserField=",
		}, cobra.ShellCompDirectiveNoSpace
	})
	return cmd
}

func newCloudAzuresDeleteCmd(ctx *registry.CLIContext) *cobra.Command {
	var (
		flagYes    bool
		flagDryRun bool
	)

	cmd := &cobra.Command{
		Use:   "delete <id>",
		Short: "Delete Cloud Identity Provider configuration.",
		Long:  "Delete Cloud Identity Provider configuration.",
		Example: `  # Delete a cloud-azure (with confirmation)
  jamf-cli pro cloud-azures delete 1

  # Delete without confirmation prompt
  jamf-cli pro cloud-azures delete 1 --yes`,
		Annotations: map[string]string{"jamf:destructive": "true", "jamf:privileges": "Delete LDAP Servers", "jamf:api": "pro"},
		Args:        cobra.ExactArgs(1),
		RunE: func(cmd *cobra.Command, args []string) error {
			reqCtx := cmd.Context()

			// Confirmation for destructive action
			if flagDryRun {
				fmt.Fprintf(os.Stderr, "Would delete resource %s\n", strings.Join(args, " "))
				return nil
			}
			if !flagYes {
				noInput, _ := cmd.Flags().GetBool("no-input")
				if noInput {
					return fmt.Errorf("destructive operation requires --yes when --no-input is set")
				}
				fmt.Fprintf(os.Stderr, "⚠️  This will delete resource %s. Type 'yes' to confirm: ", strings.Join(args, " "))
				var confirm string
				fmt.Scanln(&confirm)
				if confirm != "yes" {
					return fmt.Errorf("aborted")
				}
			}

			// Destructive cooldown enforcement
			noInputCooldown, _ := cmd.Flags().GetBool("no-input")
			if err := cooldown.Enforce(ctx.ProfileName, noInputCooldown, ctx.DestructiveCooldown); err != nil {
				return err
			}

			// Build request path
			path := "/v1/cloud-azure/{id}"
			path = strings.Replace(path, "{id}", url.PathEscape(args[0]), 1)

			// Build query string
			var queryParts []string
			if len(queryParts) > 0 {
				path = path + "?" + strings.Join(queryParts, "&")
			}

			// Make request
			resp, err := ctx.Client.Do(reqCtx, "DELETE", path, nil)
			if err != nil {
				return err
			}
			defer resp.Body.Close()

			if resp.StatusCode == http.StatusNoContent {
				cooldown.Record(ctx.ProfileName)
				fmt.Fprintln(os.Stderr, "Deleted successfully")
				return nil
			}

			err = ctx.Output.PrintResponse(resp)
			if err == nil && resp.StatusCode >= 200 && resp.StatusCode < 300 {
				cooldown.Record(ctx.ProfileName)
			}
			return err
		},
	}

	cmd.Flags().BoolVar(&flagYes, "yes", false, "Skip confirmation prompt")
	cmd.Flags().BoolVarP(&flagDryRun, "dry-run", "n", false, "Preview without executing")
	return cmd
}
