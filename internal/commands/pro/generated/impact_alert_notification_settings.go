// Copyright 2026, Jamf Software LLC
// Code generated by jamf-cli generator. DO NOT EDIT.
package generated

import (
	"bytes"
	"encoding/json"
	"fmt"
	"io"
	"os"
	"strings"

	"github.com/Jamf-Concepts/jamf-cli/internal/registry"
	"github.com/spf13/cobra"
)

// NewImpactAlertNotificationSettingsCmd creates the impact-alert-notification-settings command group
func NewImpactAlertNotificationSettingsCmd(ctx *registry.CLIContext) *cobra.Command {
	cmd := &cobra.Command{
		Use:   "impact-alert-notification-settings",
		Short: "Manage impact-alert-notification-settings",
		Long:  `Manage impact-alert-notification-settings in Jamf Pro.`,
	}

	cmd.AddCommand(newImpactAlertNotificationSettingsGetCmd(ctx))
	cmd.AddCommand(newImpactAlertNotificationSettingsUpdateCmd(ctx))

	return cmd
}

func newImpactAlertNotificationSettingsGetCmd(ctx *registry.CLIContext) *cobra.Command {
	var ()

	cmd := &cobra.Command{
		Use:   "get",
		Short: "Get Impact Alert Notification Settings",
		Long:  "Get Impact Alert Notification Settings",
		Example: `  # Get impact-alert-notification-settings
  jamf-cli pro impact-alert-notification-settings get

  # Get impact-alert-notification-settings and output as YAML
  jamf-cli pro impact-alert-notification-settings get -o yaml`,
		Annotations: map[string]string{"jamf:privileges": "Read Impact Alert Notification Settings"},
		RunE: func(cmd *cobra.Command, args []string) error {
			reqCtx := cmd.Context()

			// Build request path
			path := "/v1/impact-alert-notification-settings"

			// Build query string
			var queryParts []string
			if len(queryParts) > 0 {
				path = path + "?" + strings.Join(queryParts, "&")
			}

			// Make request
			resp, err := ctx.Client.Do(reqCtx, "GET", path, nil)
			if err != nil {
				return err
			}
			defer resp.Body.Close()

			return ctx.Output.PrintResponse(resp)
		},
	}

	return cmd
}

func newImpactAlertNotificationSettingsUpdateCmd(ctx *registry.CLIContext) *cobra.Command {
	var (
		flagScaffold bool
		flagSet      []string
	)

	cmd := &cobra.Command{
		Use:   "update",
		Short: "Update Impact Alert Notification Settings",
		Long:  "Update Impact Alert Notification Settings\n\nUse --set KEY=VALUE to update individual fields (repeatable). The current resource is fetched, your changes are merged in, read-only fields are dropped, and the whole record is written back. Omitted fields keep their current values.\n\nAvailable fields:\n  deployableObjectsAlertEnabled                boolean\n  deployableObjectsConfirmationCodeEnabled     boolean\n  scopeableObjectsAlertEnabled                 boolean\n  scopeableObjectsConfirmationCodeEnabled      boolean\n\nWithout --set, pipe a full JSON document to stdin to replace the resource entirely.",
		Example: `  # Update individual fields (fetch-merge-replace)
  jamf-cli pro impact-alert-notification-settings update --set field=value

  # Replace impact-alert-notification-settings from a full JSON document
  jamf-cli pro impact-alert-notification-settings get -o json | jq '.field = "value"' | jamf-cli pro impact-alert-notification-settings update

  # Update from a file
  jamf-cli pro impact-alert-notification-settings update --from-file impact-alert-notification-settings.json`,
		Annotations: map[string]string{"jamf:privileges": "Update Impact Alert Notification Settings"},
		RunE: func(cmd *cobra.Command, args []string) error {
			reqCtx := cmd.Context()

			if flagScaffold {
				return printScaffoldOutput(`{
  "deployableObjectsAlertEnabled": false,
  "deployableObjectsConfirmationCodeEnabled": false,
  "scopeableObjectsAlertEnabled": false,
  "scopeableObjectsConfirmationCodeEnabled": false
}`, ctx.Output.Format())
			}

			// Build request path
			path := "/v1/impact-alert-notification-settings"

			// Build query string
			var queryParts []string
			if len(queryParts) > 0 {
				path = path + "?" + strings.Join(queryParts, "&")
			}

			// Make request
			// Read body from stdin if available
			var body io.Reader
			var normalized []byte
			if len(flagSet) > 0 {
				// --set: fetch current state, drop read-only / server-computed fields,
				// merge the caller's changes, and PUT the full record back. Fields not
				// named in --set keep their current values.
				existing, ferr := fetchForMerge(reqCtx, ctx.Client, path)
				if ferr != nil {
					return ferr
				}
				current := map[string]any{}
				if len(existing) > 0 {
					if err := json.Unmarshal(existing, &current); err != nil {
						return fmt.Errorf("parsing current impact-alert-notification-settings for --set: %w", err)
					}
				}
				(&fieldFilter{fields: map[string]*fieldFilter{"deployableObjectsAlertEnabled": nil, "deployableObjectsConfirmationCodeEnabled": nil, "scopeableObjectsAlertEnabled": nil, "scopeableObjectsConfirmationCodeEnabled": nil}}).apply(current)
				setDoc, serr := buildMergePatchFromSet(flagSet)
				if serr != nil {
					return serr
				}
				setMap := map[string]any{}
				if err := json.Unmarshal(setDoc, &setMap); err != nil {
					return err
				}
				deepMergeJSON(current, setMap)
				merged, merr := json.Marshal(current)
				if merr != nil {
					return merr
				}
				normalized = merged
			}
			stat, _ := os.Stdin.Stat()
			if len(flagSet) == 0 && (stat.Mode()&os.ModeCharDevice) == 0 {
				raw, err := io.ReadAll(io.LimitReader(os.Stdin, 10<<20))
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				normalized, err = normalizeInputToJSON(raw)
				if err != nil {
					return err
				}
			}
			if len(normalized) > 0 {
				body = bytes.NewReader(normalized)
			}
			resp, err := ctx.Client.Do(reqCtx, "PUT", path, body)
			if err != nil {
				return err
			}
			defer resp.Body.Close()

			return ctx.Output.PrintResponse(resp)
		},
	}

	cmd.Flags().BoolVar(&flagScaffold, "scaffold", false, "Print a JSON template for the request body and exit")
	cmd.Flags().StringArrayVar(&flagSet, "set", nil, "Update a field via fetch-merge-replace (key=value in dot notation, repeatable)")
	_ = cmd.RegisterFlagCompletionFunc("set", func(_ *cobra.Command, _ []string, _ string) ([]string, cobra.ShellCompDirective) {
		return []string{
			"deployableObjectsAlertEnabled=", "deployableObjectsConfirmationCodeEnabled=", "scopeableObjectsAlertEnabled=", "scopeableObjectsConfirmationCodeEnabled=",
		}, cobra.ShellCompDirectiveNoSpace
	})
	return cmd
}
