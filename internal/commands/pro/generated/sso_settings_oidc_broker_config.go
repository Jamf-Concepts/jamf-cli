// Copyright 2026, Jamf Software LLC
// Code generated by jamf-cli generator. DO NOT EDIT.
package generated

import (
	"bytes"
	"encoding/json"
	"fmt"
	"io"
	"os"
	"strings"

	"github.com/Jamf-Concepts/jamf-cli/internal/registry"
	"github.com/spf13/cobra"
)

// NewSsoSettingsOidcBrokerConfigCmd creates the sso-settings oidc-broker-config command group
func NewSsoSettingsOidcBrokerConfigCmd(ctx *registry.CLIContext) *cobra.Command {
	cmd := &cobra.Command{
		Use:         "oidc-broker-config",
		Short:       "Manage sso-settings oidc-broker-config",
		Long:        `Manage sso-settings oidc-broker-config in Jamf Pro.`,
		Annotations: map[string]string{"jamf:api": "pro"},
	}

	cmd.AddCommand(newSsoSettingsOidcBrokerConfigGetCmd(ctx))
	cmd.AddCommand(newSsoSettingsOidcBrokerConfigUpdateCmd(ctx))

	return cmd
}

func newSsoSettingsOidcBrokerConfigGetCmd(ctx *registry.CLIContext) *cobra.Command {
	var ()

	cmd := &cobra.Command{
		Use:   "get",
		Short: "Get the OIDC broker configuration",
		Long:  "Returns the tenant's currently selected OIDC broker configuration from the remote authentication service. Secret fields are never included in the response.",
		Example: `  # Get sso-settings oidc-broker-config
  jamf-cli pro sso-settings oidc-broker-config get

  # Get sso-settings oidc-broker-config and output as YAML
  jamf-cli pro sso-settings oidc-broker-config get -o yaml`,
		Annotations: map[string]string{"jamf:privileges": "Read SSO Settings", "jamf:api": "pro", "jamf:gateway-privileges": "sso-settings:read"},
		RunE: func(cmd *cobra.Command, args []string) error {
			reqCtx := cmd.Context()

			// Build request path
			path := "/v3/sso/oidc-broker-config"

			// Build query string
			var queryParts []string
			if len(queryParts) > 0 {
				path = path + "?" + strings.Join(queryParts, "&")
			}

			// Make request
			resp, err := ctx.Client.Do(reqCtx, "GET", path, nil)
			if err != nil {
				return err
			}
			defer resp.Body.Close()

			return ctx.Output.PrintResponse(resp)
		},
	}

	return cmd
}

func newSsoSettingsOidcBrokerConfigUpdateCmd(ctx *registry.CLIContext) *cobra.Command {
	var (
		flagScaffold bool
		flagSet      []string
	)

	cmd := &cobra.Command{
		Use:   "update",
		Short: "Update the OIDC broker configuration",
		Long:  "Full-replacement update of the tenant's selected broker configuration's contents on the\nremote authentication service. Omit clientSecret/privateKeyJwt to keep the currently\nstored value; supply either to rotate it — except when this request changes\nclientAuthMethod, which requires the new method's credential to be supplied, because the\nauthentication service clears the credential belonging to the method being left. The\nconfiguration is always written as an ADMIN_SSO capability, CONFIDENTIAL client; neither\nvalue is settable through this request.\n\nUse --set KEY=VALUE to update individual fields (repeatable). The current resource is fetched, your changes are merged in, read-only fields are dropped, and the whole record is written back. Omitted fields keep their current values.\n\nAvailable fields:\n  clientAuthMethod                             string\n  clientId                                     string\n  clientSecret                                 string\n  discoveryUrl                                 string\n  enabled                                      boolean\n  privateKeyJwt                                string\n  productUserMapping                           string\n  productUsernameClaim                         string\n\nArray and object fields accept a JSON value (e.g. --set field='[\"a\",\"b\"]'):\n  scopes                                       array\n\nWithout --set, pipe a full JSON document to stdin to replace the resource entirely.",
		Example: `  # Update individual fields (fetch-merge-replace)
  jamf-cli pro sso-settings oidc-broker-config update --set field=value

  # Replace sso-settings oidc-broker-config from a full JSON document
  jamf-cli pro sso-settings oidc-broker-config get -o json | jq '.field = "value"' | jamf-cli pro sso-settings oidc-broker-config update

  # Update from a file
  jamf-cli pro sso-settings oidc-broker-config update --from-file sso-settings-oidc-broker-config.json`,
		Annotations: map[string]string{"jamf:privileges": "Update SSO Settings", "jamf:api": "pro", "jamf:gateway-privileges": "sso-settings:update"},
		RunE: func(cmd *cobra.Command, args []string) error {
			reqCtx := cmd.Context()

			if flagScaffold {
				return printScaffoldOutput(`{
  "clientAuthMethod": "CLIENT_SECRET",
  "clientId": "my-client-id",
  "clientSecret": "***",
  "discoveryUrl": "https://example.okta.com/.well-known/openid-configuration",
  "enabled": true,
  "privateKeyJwt": "***",
  "productUserMapping": "EMAIL",
  "productUsernameClaim": "email",
  "scopes": [
    "openid",
    "profile"
  ]
}`, ctx.Output.Format())
			}

			// Build request path
			path := "/v3/sso/oidc-broker-config"

			// Build query string
			var queryParts []string
			if len(queryParts) > 0 {
				path = path + "?" + strings.Join(queryParts, "&")
			}

			// Make request
			// Read body from stdin if available
			var body io.Reader
			var normalized []byte
			if len(flagSet) > 0 {
				// --set: fetch current state, drop read-only / server-computed fields,
				// merge the caller's changes, and PUT the full record back. Fields not
				// named in --set keep their current values.
				existing, ferr := fetchForMerge(reqCtx, ctx.Client, path)
				if ferr != nil {
					return ferr
				}
				current := map[string]any{}
				if len(existing) > 0 {
					if err := json.Unmarshal(existing, &current); err != nil {
						return fmt.Errorf("parsing current oidc-broker-config for --set: %w", err)
					}
				}
				(&fieldFilter{fields: map[string]*fieldFilter{"clientAuthMethod": nil, "clientId": nil, "clientSecret": nil, "discoveryUrl": nil, "enabled": nil, "privateKeyJwt": nil, "productUserMapping": nil, "productUsernameClaim": nil, "scopes": nil}}).apply(current)
				setDoc, serr := buildMergePatchFromSet(flagSet, map[string]string{"clientAuthMethod": "string", "clientId": "string", "clientSecret": "string", "discoveryUrl": "string", "enabled": "boolean", "privateKeyJwt": "string", "productUserMapping": "string", "productUsernameClaim": "string", "scopes": "array"})
				if serr != nil {
					return serr
				}
				setMap := map[string]any{}
				if err := json.Unmarshal(setDoc, &setMap); err != nil {
					return err
				}
				if !hasNestedKey(setMap, "clientSecret") {
					fmt.Fprintf(os.Stderr, "warning: oidc-broker-config field %q is write-only: the server never returns it, so this update will blank any existing value. Pass --set clientSecret=<value> to preserve it.\n", "clientSecret")
				}
				if !hasNestedKey(setMap, "privateKeyJwt") {
					fmt.Fprintf(os.Stderr, "warning: oidc-broker-config field %q is write-only: the server never returns it, so this update will blank any existing value. Pass --set privateKeyJwt=<value> to preserve it.\n", "privateKeyJwt")
				}
				deepMergeJSON(current, setMap)
				merged, merr := json.Marshal(current)
				if merr != nil {
					return merr
				}
				normalized = merged
				if setStat, _ := os.Stdin.Stat(); setStat != nil && (setStat.Mode()&os.ModeCharDevice) == 0 {
					fmt.Fprintln(os.Stderr, "warning: --set and piped stdin are mutually exclusive; ignoring stdin")
				}
			}
			stat, _ := os.Stdin.Stat()
			if len(flagSet) == 0 && (stat.Mode()&os.ModeCharDevice) == 0 {
				raw, err := io.ReadAll(io.LimitReader(os.Stdin, 10<<20))
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				normalized, err = normalizeInputToJSON(raw)
				if err != nil {
					return err
				}
			}
			if len(normalized) > 0 {
				body = bytes.NewReader(normalized)
			}
			resp, err := ctx.Client.Do(reqCtx, "PUT", path, body)
			if err != nil {
				return err
			}
			defer resp.Body.Close()

			return ctx.Output.PrintResponse(resp)
		},
	}

	cmd.Flags().BoolVar(&flagScaffold, "scaffold", false, "Print a JSON template for the request body and exit")
	cmd.Flags().StringArrayVar(&flagSet, "set", nil, "Update a field via fetch-merge-replace (key=value in dot notation, repeatable)")
	_ = cmd.RegisterFlagCompletionFunc("set", func(_ *cobra.Command, _ []string, _ string) ([]string, cobra.ShellCompDirective) {
		return []string{
			"clientAuthMethod=", "clientId=", "clientSecret=", "discoveryUrl=", "enabled=", "privateKeyJwt=", "productUserMapping=", "productUsernameClaim=",
		}, cobra.ShellCompDirectiveNoSpace
	})
	return cmd
}
