// Copyright 2026, Jamf Software LLC
// Code generated by jamf-cli generator. DO NOT EDIT.
package generated

import (
	"bytes"
	"encoding/json"
	"fmt"
	"io"
	"os"
	"strings"

	"github.com/Jamf-Concepts/jamf-cli/internal/registry"
	"github.com/spf13/cobra"
)

// NewManagedSoftwareUpdatesPlansFeatureToggleCmd creates the managed-software-updates-plans feature-toggle command group
func NewManagedSoftwareUpdatesPlansFeatureToggleCmd(ctx *registry.CLIContext) *cobra.Command {
	cmd := &cobra.Command{
		Use:         "feature-toggle",
		Short:       "Manage managed-software-updates-plans feature-toggle",
		Long:        `Manage managed-software-updates-plans feature-toggle in Jamf Pro.`,
		Annotations: map[string]string{"jamf:api": "pro"},
	}

	cmd.AddCommand(newManagedSoftwareUpdatesPlansFeatureToggleGetCmd(ctx))
	cmd.AddCommand(newManagedSoftwareUpdatesPlansFeatureToggleUpdateCmd(ctx))
	cmd.AddCommand(newManagedSoftwareUpdatesPlansFeatureToggleAbandonCmd(ctx))
	cmd.AddCommand(newManagedSoftwareUpdatesPlansFeatureToggleStatusCmd(ctx))

	return cmd
}

func newManagedSoftwareUpdatesPlansFeatureToggleGetCmd(ctx *registry.CLIContext) *cobra.Command {
	var ()

	cmd := &cobra.Command{
		Use:   "get",
		Short: "Retrieve current value of the Feature Toggle",
		Long:  "Retrieves current value of the Feature Toggle",
		Example: `  # Get managed-software-updates-plans feature-toggle
  jamf-cli pro managed-software-updates-plans feature-toggle get

  # Get managed-software-updates-plans feature-toggle and output as YAML
  jamf-cli pro managed-software-updates-plans feature-toggle get -o yaml`,
		Annotations: map[string]string{"jamf:privileges": "Read Managed Software Updates", "jamf:api": "pro", "jamf:gateway-privileges": "managed-software-updates:read"},
		RunE: func(cmd *cobra.Command, args []string) error {
			reqCtx := cmd.Context()

			// Build request path
			path := "/v1/managed-software-updates/plans/feature-toggle"

			// Build query string
			var queryParts []string
			if len(queryParts) > 0 {
				path = path + "?" + strings.Join(queryParts, "&")
			}

			// Make request
			resp, err := ctx.Client.Do(reqCtx, "GET", path, nil)
			if err != nil {
				return err
			}
			defer resp.Body.Close()

			return ctx.Output.PrintResponse(resp)
		},
	}

	return cmd
}

func newManagedSoftwareUpdatesPlansFeatureToggleUpdateCmd(ctx *registry.CLIContext) *cobra.Command {
	var (
		flagScaffold bool
		flagSet      []string
	)

	cmd := &cobra.Command{
		Use:   "update",
		Short: "Updates Feature Toggle Value",
		Long:  "Updates the value of the Feature Toggle - This endpoint is asynchronous, the provided value will not be immediately updated. Please use the following endpoint to track the status of your toggle request. /v1/managed-software-updates/plans/feature-toggle/status:\n\nUse --set KEY=VALUE to update individual fields (repeatable). The current resource is fetched, your changes are merged in, read-only fields are dropped, and the whole record is written back. Omitted fields keep their current values.\n\nAvailable fields:\n  toggle                                       boolean\n\nWithout --set, pipe a full JSON document to stdin to replace the resource entirely.",
		Example: `  # Update individual fields (fetch-merge-replace)
  jamf-cli pro managed-software-updates-plans feature-toggle update --set field=value

  # Replace managed-software-updates-plans feature-toggle from a full JSON document
  jamf-cli pro managed-software-updates-plans feature-toggle get -o json | jq '.field = "value"' | jamf-cli pro managed-software-updates-plans feature-toggle update

  # Update from a file
  jamf-cli pro managed-software-updates-plans feature-toggle update --from-file managed-software-updates-plans-feature-toggle.json`,
		Annotations: map[string]string{"jamf:privileges": "Read Managed Software Updates,Create Managed Software Updates,Update Managed Software Updates", "jamf:api": "pro", "jamf:gateway-privileges": "managed-software-updates:create,managed-software-updates:read,managed-software-updates:update"},
		RunE: func(cmd *cobra.Command, args []string) error {
			reqCtx := cmd.Context()

			if flagScaffold {
				return printScaffoldOutput(`{
  "toggle": false
}`, ctx.Output.Format())
			}

			// Build request path
			path := "/v1/managed-software-updates/plans/feature-toggle"

			// Build query string
			var queryParts []string
			if len(queryParts) > 0 {
				path = path + "?" + strings.Join(queryParts, "&")
			}

			// Make request
			// Read body from stdin if available
			var body io.Reader
			var normalized []byte
			if len(flagSet) > 0 {
				// --set: fetch current state, drop read-only / server-computed fields,
				// merge the caller's changes, and PUT the full record back. Fields not
				// named in --set keep their current values.
				existing, ferr := fetchForMerge(reqCtx, ctx.Client, path)
				if ferr != nil {
					return ferr
				}
				current := map[string]any{}
				if len(existing) > 0 {
					if err := json.Unmarshal(existing, &current); err != nil {
						return fmt.Errorf("parsing current feature-toggle for --set: %w", err)
					}
				}
				(&fieldFilter{fields: map[string]*fieldFilter{"toggle": nil}}).apply(current)
				setDoc, serr := buildMergePatchFromSet(flagSet, map[string]string{"toggle": "boolean"})
				if serr != nil {
					return serr
				}
				setMap := map[string]any{}
				if err := json.Unmarshal(setDoc, &setMap); err != nil {
					return err
				}
				deepMergeJSON(current, setMap)
				merged, merr := json.Marshal(current)
				if merr != nil {
					return merr
				}
				normalized = merged
				if setStat, _ := os.Stdin.Stat(); setStat != nil && (setStat.Mode()&os.ModeCharDevice) == 0 {
					fmt.Fprintln(os.Stderr, "warning: --set and piped stdin are mutually exclusive; ignoring stdin")
				}
			}
			stat, _ := os.Stdin.Stat()
			if len(flagSet) == 0 && (stat.Mode()&os.ModeCharDevice) == 0 {
				raw, err := io.ReadAll(io.LimitReader(os.Stdin, 10<<20))
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				normalized, err = normalizeInputToJSON(raw)
				if err != nil {
					return err
				}
			}
			if len(normalized) > 0 {
				body = bytes.NewReader(normalized)
			}
			resp, err := ctx.Client.Do(reqCtx, "PUT", path, body)
			if err != nil {
				return err
			}
			defer resp.Body.Close()

			return ctx.Output.PrintResponse(resp)
		},
	}

	cmd.Flags().BoolVar(&flagScaffold, "scaffold", false, "Print a JSON template for the request body and exit")
	cmd.Flags().StringArrayVar(&flagSet, "set", nil, "Update a field via fetch-merge-replace (key=value in dot notation, repeatable)")
	_ = cmd.RegisterFlagCompletionFunc("set", func(_ *cobra.Command, _ []string, _ string) ([]string, cobra.ShellCompDirective) {
		return []string{
			"toggle=",
		}, cobra.ShellCompDirectiveNoSpace
	})
	return cmd
}

func newManagedSoftwareUpdatesPlansFeatureToggleAbandonCmd(ctx *registry.CLIContext) *cobra.Command {
	var ()

	cmd := &cobra.Command{
		Use:         "abandon",
		Short:       "Force stops any ongoing or stalled feature-toggle processes",
		Long:        "\"Break Glass\" endpoint, not for nominal usage. Use this endpoint to forcefully abandon the feature-toggle background process if the status of the feature-toggle is 'stuck' or has reached an non-restartable failed state. Usage of this endpoint under nominal conditions is undefined and unsupported.",
		Annotations: map[string]string{"jamf:privileges": "Read Managed Software Updates,Create Managed Software Updates,Update Managed Software Updates", "jamf:api": "pro", "jamf:gateway-privileges": "managed-software-updates:create,managed-software-updates:read,managed-software-updates:update"},
		RunE: func(cmd *cobra.Command, args []string) error {
			reqCtx := cmd.Context()

			// Build request path
			path := "/v1/managed-software-updates/plans/feature-toggle/abandon"

			// Build query string
			var queryParts []string
			if len(queryParts) > 0 {
				path = path + "?" + strings.Join(queryParts, "&")
			}

			// Make request
			// Read body from stdin if available
			var body io.Reader
			var normalized []byte
			stat, _ := os.Stdin.Stat()
			if (stat.Mode() & os.ModeCharDevice) == 0 {
				raw, err := io.ReadAll(io.LimitReader(os.Stdin, 10<<20))
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				normalized, err = normalizeInputToJSON(raw)
				if err != nil {
					return err
				}
			}
			if len(normalized) > 0 {
				body = bytes.NewReader(normalized)
			}
			resp, err := ctx.Client.Do(reqCtx, "POST", path, body)
			if err != nil {
				return err
			}
			defer resp.Body.Close()

			return ctx.Output.PrintResponse(resp)
		},
	}

	return cmd
}

func newManagedSoftwareUpdatesPlansFeatureToggleStatusCmd(ctx *registry.CLIContext) *cobra.Command {
	var ()

	cmd := &cobra.Command{
		Use:         "status",
		Short:       "Retrieves background status of the Feature Toggle",
		Long:        "Retrieves background status of the Feature Toggle",
		Annotations: map[string]string{"jamf:privileges": "Read Managed Software Updates", "jamf:api": "pro", "jamf:gateway-privileges": "managed-software-updates:read"},
		RunE: func(cmd *cobra.Command, args []string) error {
			reqCtx := cmd.Context()

			// Build request path
			path := "/v1/managed-software-updates/plans/feature-toggle/status"

			// Build query string
			var queryParts []string
			if len(queryParts) > 0 {
				path = path + "?" + strings.Join(queryParts, "&")
			}

			// Make request
			resp, err := ctx.Client.Do(reqCtx, "GET", path, nil)
			if err != nil {
				return err
			}
			defer resp.Body.Close()

			return ctx.Output.PrintResponse(resp)
		},
	}

	return cmd
}
