// Copyright 2026, Jamf Software LLC
// Code generated by jamf-cli generator. DO NOT EDIT.
package generated

import (
	"bytes"
	"encoding/json"
	"fmt"
	"io"
	"os"
	"strings"

	"github.com/Jamf-Concepts/jamf-cli/internal/registry"
	"github.com/spf13/cobra"
)

// NewServiceDiscoveryEnrollmentCmd creates the service-discovery-enrollment command group
func NewServiceDiscoveryEnrollmentCmd(ctx *registry.CLIContext) *cobra.Command {
	cmd := &cobra.Command{
		Use:         "service-discovery-enrollment",
		Short:       "Manage service-discovery-enrollment",
		Long:        `Manage service-discovery-enrollment in Jamf Pro.`,
		Annotations: map[string]string{"jamf:api": "pro"},
	}

	cmd.AddCommand(newServiceDiscoveryEnrollmentGetCmd(ctx))
	cmd.AddCommand(newServiceDiscoveryEnrollmentUpdateCmd(ctx))

	return cmd
}

func newServiceDiscoveryEnrollmentGetCmd(ctx *registry.CLIContext) *cobra.Command {
	var ()

	cmd := &cobra.Command{
		Use:   "get",
		Short: "Get service discovery well-known settings for all organizations",
		Long:  "Returns current settings for all AxM organizations.",
		Example: `  # Get service-discovery-enrollment
  jamf-cli pro service-discovery-enrollment get

  # Get service-discovery-enrollment and output as YAML
  jamf-cli pro service-discovery-enrollment get -o yaml`,
		Annotations: map[string]string{"jamf:privileges": "Read User-Initiated Enrollment", "jamf:api": "pro", "jamf:gateway-privileges": "user-initiated-enrollment:read"},
		RunE: func(cmd *cobra.Command, args []string) error {
			reqCtx := cmd.Context()

			// Build request path
			path := "/v1/service-discovery-enrollment/well-known-settings"

			// Build query string
			var queryParts []string
			if len(queryParts) > 0 {
				path = path + "?" + strings.Join(queryParts, "&")
			}

			// Make request
			resp, err := ctx.Client.Do(reqCtx, "GET", path, nil)
			if err != nil {
				return err
			}
			defer resp.Body.Close()

			return ctx.Output.PrintResponse(resp)
		},
	}

	return cmd
}

func newServiceDiscoveryEnrollmentUpdateCmd(ctx *registry.CLIContext) *cobra.Command {
	var (
		flagScaffold bool
		flagSet      []string
	)

	cmd := &cobra.Command{
		Use:   "update",
		Short: "Update service discovery well-known settings",
		Long:  "Accepts JSON payload to update enrollment types for AxM organizations.\nRequires \"Update User-Initiated Enrollment\" privilege.\nArray and object fields accept a JSON value (e.g. --set field='[\"a\",\"b\"]'):\n  wellKnownSettings                            array\n\nWithout --set, pipe a full JSON document to stdin to replace the resource entirely.",
		Example: `  # Update individual fields (fetch-merge-replace)
  jamf-cli pro service-discovery-enrollment update --set field=value

  # Replace service-discovery-enrollment from a full JSON document
  jamf-cli pro service-discovery-enrollment get -o json | jq '.field = "value"' | jamf-cli pro service-discovery-enrollment update

  # Update from a file
  jamf-cli pro service-discovery-enrollment update --from-file service-discovery-enrollment.json`,
		Annotations: map[string]string{"jamf:privileges": "Update User-Initiated Enrollment", "jamf:api": "pro", "jamf:gateway-privileges": "user-initiated-enrollment:update"},
		RunE: func(cmd *cobra.Command, args []string) error {
			reqCtx := cmd.Context()

			if flagScaffold {
				return printScaffoldOutput(`{
  "wellKnownSettings": [
    {
      "enrollmentType": "mdm-byod",
      "orgName": "Acme Corporation",
      "serverUuid": "a1b2c3d4-e5f6-7890-abcd-ef1234567890"
    }
  ]
}`, ctx.Output.Format())
			}

			// Build request path
			path := "/v1/service-discovery-enrollment/well-known-settings"

			// Build query string
			var queryParts []string
			if len(queryParts) > 0 {
				path = path + "?" + strings.Join(queryParts, "&")
			}

			// Make request
			// Read body from stdin if available
			var body io.Reader
			var normalized []byte
			if len(flagSet) > 0 {
				// --set: fetch current state, drop read-only / server-computed fields,
				// merge the caller's changes, and PUT the full record back. Fields not
				// named in --set keep their current values.
				existing, ferr := fetchForMerge(reqCtx, ctx.Client, path)
				if ferr != nil {
					return ferr
				}
				current := map[string]any{}
				if len(existing) > 0 {
					if err := json.Unmarshal(existing, &current); err != nil {
						return fmt.Errorf("parsing current service-discovery-enrollment for --set: %w", err)
					}
				}
				(&fieldFilter{fields: map[string]*fieldFilter{"wellKnownSettings": nil}}).apply(current)
				setDoc, serr := buildMergePatchFromSet(flagSet, map[string]string{"wellKnownSettings": "array"})
				if serr != nil {
					return serr
				}
				setMap := map[string]any{}
				if err := json.Unmarshal(setDoc, &setMap); err != nil {
					return err
				}
				deepMergeJSON(current, setMap)
				merged, merr := json.Marshal(current)
				if merr != nil {
					return merr
				}
				normalized = merged
				if setStat, _ := os.Stdin.Stat(); setStat != nil && (setStat.Mode()&os.ModeCharDevice) == 0 {
					fmt.Fprintln(os.Stderr, "warning: --set and piped stdin are mutually exclusive; ignoring stdin")
				}
			}
			stat, _ := os.Stdin.Stat()
			if len(flagSet) == 0 && (stat.Mode()&os.ModeCharDevice) == 0 {
				raw, err := io.ReadAll(io.LimitReader(os.Stdin, 10<<20))
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				normalized, err = normalizeInputToJSON(raw)
				if err != nil {
					return err
				}
			}
			if len(normalized) > 0 {
				body = bytes.NewReader(normalized)
			}
			resp, err := ctx.Client.Do(reqCtx, "PUT", path, body)
			if err != nil {
				return err
			}
			defer resp.Body.Close()

			return ctx.Output.PrintResponse(resp)
		},
	}

	cmd.Flags().BoolVar(&flagScaffold, "scaffold", false, "Print a JSON template for the request body and exit")
	cmd.Flags().StringArrayVar(&flagSet, "set", nil, "Update a field via fetch-merge-replace (key=value in dot notation, repeatable)")
	_ = cmd.RegisterFlagCompletionFunc("set", func(_ *cobra.Command, _ []string, _ string) ([]string, cobra.ShellCompDirective) {
		return []string{}, cobra.ShellCompDirectiveNoSpace
	})
	return cmd
}
