// Code generated by jamfpro-cli generator. DO NOT EDIT.
package generated

import (
	"context"
	"fmt"
	"io"
	"os"
	"strings"

	"github.com/spf13/cobra"
)

// NewChangePasswordsCmd creates the change-passwords command group
func NewChangePasswordsCmd(ctx *CLIContext) *cobra.Command {
	cmd := &cobra.Command{
		Use:   "change-passwords",
		Short: "Manage change-passwords",
		Long:  `Manage change-passwords in Jamf Pro.`,
	}

	cmd.AddCommand(newChangePasswordsChangePasswordCmd(ctx))

	return cmd
}

func newChangePasswordsChangePasswordCmd(ctx *CLIContext) *cobra.Command {
	var (
		flagScaffold bool
	)

	cmd := &cobra.Command{
		Use:   "change-password",
		Short: "Changes the user account password.",
		Long:  "Changes the account password for a currently authenticated user.",
		RunE: func(cmd *cobra.Command, args []string) error {
			reqCtx := context.Background()

			if flagScaffold {
				fmt.Println(`{
  "currentPassword": "oldPassword",
  "newPassword": "updatedPassword"
}`)
				return nil
			}

			// Build request path
			path := "/v1/user/change-password"

			// Build query string
			var queryParts []string
			if len(queryParts) > 0 {
				path = path + "?" + strings.Join(queryParts, "&")
			}

			// Make request
			// Read body from stdin if available
			var body io.Reader
			stat, _ := os.Stdin.Stat()
			if (stat.Mode() & os.ModeCharDevice) == 0 {
				body = os.Stdin
			}
			resp, err := ctx.Client.Do(reqCtx, "POST", path, body)
			if err != nil {
				return err
			}
			defer resp.Body.Close()

			return ctx.Output.PrintResponse(resp)
		},
	}

	cmd.Flags().BoolVar(&flagScaffold, "scaffold", false, "Print a JSON template for the request body and exit")

	return cmd
}
