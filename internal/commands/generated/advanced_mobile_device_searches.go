// Code generated by jamfpro-cli generator. DO NOT EDIT.
package generated

import (
	"context"
	"fmt"
	"io"
	"net/http"
	"os"
	"strings"

	"github.com/spf13/cobra"
)

// NewAdvancedMobileDeviceSearchesCmd creates the advanced-mobile-device-searches command group
func NewAdvancedMobileDeviceSearchesCmd(ctx *CLIContext) *cobra.Command {
	cmd := &cobra.Command{
		Use:   "advanced-mobile-device-searches",
		Short: "Manage advanced-mobile-device-searches",
		Long:  `Manage advanced-mobile-device-searches in Jamf Pro.`,
	}

	cmd.AddCommand(newAdvancedMobileDeviceSearchesListCmd(ctx))
	cmd.AddCommand(newAdvancedMobileDeviceSearchesGetCmd(ctx))
	cmd.AddCommand(newAdvancedMobileDeviceSearchesCreateCmd(ctx))
	cmd.AddCommand(newAdvancedMobileDeviceSearchesUpdateCmd(ctx))
	cmd.AddCommand(newAdvancedMobileDeviceSearchesDeleteCmd(ctx))
	cmd.AddCommand(newAdvancedMobileDeviceSearchesDeleteMultipleCmd(ctx))

	return cmd
}

func newAdvancedMobileDeviceSearchesListCmd(ctx *CLIContext) *cobra.Command {
	var (
	)

	cmd := &cobra.Command{
		Use:   "list",
		Short: "Get Advanced Search objects",
		Long:  "Gets Advanced Search Objects",
		RunE: func(cmd *cobra.Command, args []string) error {
			reqCtx := context.Background()

			// Build request path
			path := "/v1/advanced-mobile-device-searches"

			// Build query string
			var queryParts []string
			if len(queryParts) > 0 {
				path = path + "?" + strings.Join(queryParts, "&")
			}

			// Make request
			resp, err := ctx.Client.Do(reqCtx, "GET", path, nil)
			if err != nil {
				return err
			}
			defer resp.Body.Close()


			return ctx.Output.PrintResponse(resp)
		},
	}


	return cmd
}

func newAdvancedMobileDeviceSearchesGetCmd(ctx *CLIContext) *cobra.Command {
	var (
	)

	cmd := &cobra.Command{
		Use:   "get <id>",
		Short: "Get specified Advanced Search object",
		Long:  "Gets Specified Advanced Search Object",
		Args:  cobra.ExactArgs(1),
		RunE: func(cmd *cobra.Command, args []string) error {
			reqCtx := context.Background()

			// Build request path
			path := "/v1/advanced-mobile-device-searches/{id}"
			path = strings.Replace(path, "{id}", args[0], 1)

			// Build query string
			var queryParts []string
			if len(queryParts) > 0 {
				path = path + "?" + strings.Join(queryParts, "&")
			}

			// Make request
			resp, err := ctx.Client.Do(reqCtx, "GET", path, nil)
			if err != nil {
				return err
			}
			defer resp.Body.Close()


			return ctx.Output.PrintResponse(resp)
		},
	}


	return cmd
}

func newAdvancedMobileDeviceSearchesCreateCmd(ctx *CLIContext) *cobra.Command {
	var (
	)

	cmd := &cobra.Command{
		Use:   "create",
		Short: "Create Advanced Search object",
		Long:  "Creates Advanced Search Object",
		RunE: func(cmd *cobra.Command, args []string) error {
			reqCtx := context.Background()

			// Build request path
			path := "/v1/advanced-mobile-device-searches"

			// Build query string
			var queryParts []string
			if len(queryParts) > 0 {
				path = path + "?" + strings.Join(queryParts, "&")
			}

			// Make request
			// Read body from stdin if available
			var body io.Reader
			stat, _ := os.Stdin.Stat()
			if (stat.Mode() & os.ModeCharDevice) == 0 {
				body = os.Stdin
			}
			resp, err := ctx.Client.Do(reqCtx, "POST", path, body)
			if err != nil {
				return err
			}
			defer resp.Body.Close()


			return ctx.Output.PrintResponse(resp)
		},
	}


	return cmd
}

func newAdvancedMobileDeviceSearchesUpdateCmd(ctx *CLIContext) *cobra.Command {
	var (
	)

	cmd := &cobra.Command{
		Use:   "update <id>",
		Short: "Get specified Advanced Search object",
		Long:  "Gets Specified Advanced Search Object",
		Args:  cobra.ExactArgs(1),
		RunE: func(cmd *cobra.Command, args []string) error {
			reqCtx := context.Background()

			// Build request path
			path := "/v1/advanced-mobile-device-searches/{id}"
			path = strings.Replace(path, "{id}", args[0], 1)

			// Build query string
			var queryParts []string
			if len(queryParts) > 0 {
				path = path + "?" + strings.Join(queryParts, "&")
			}

			// Make request
			// Read body from stdin if available
			var body io.Reader
			stat, _ := os.Stdin.Stat()
			if (stat.Mode() & os.ModeCharDevice) == 0 {
				body = os.Stdin
			}
			resp, err := ctx.Client.Do(reqCtx, "PUT", path, body)
			if err != nil {
				return err
			}
			defer resp.Body.Close()


			return ctx.Output.PrintResponse(resp)
		},
	}


	return cmd
}

func newAdvancedMobileDeviceSearchesDeleteCmd(ctx *CLIContext) *cobra.Command {
	var (
		flagYes bool
		flagDryRun bool
	)

	cmd := &cobra.Command{
		Use:   "delete <id>",
		Short: "Remove specified Advanced Search object",
		Long:  "Removes specified Advanced Search Object",
		Args:  cobra.ExactArgs(1),
		RunE: func(cmd *cobra.Command, args []string) error {
			reqCtx := context.Background()

			// Confirmation for destructive action
			if flagDryRun {
				fmt.Fprintf(os.Stderr, "Would delete resource %s\n", args[0])
				return nil
			}
			if !flagYes {
				noInput, _ := cmd.Flags().GetBool("no-input")
				if noInput {
					return fmt.Errorf("destructive operation requires --yes when --no-input is set")
				}
				fmt.Fprintf(os.Stderr, "⚠️  This will delete resource %s. Type 'yes' to confirm: ", args[0])
				var confirm string
				fmt.Scanln(&confirm)
				if confirm != "yes" {
					return fmt.Errorf("aborted")
				}
			}

			// Build request path
			path := "/v1/advanced-mobile-device-searches/{id}"
			path = strings.Replace(path, "{id}", args[0], 1)

			// Build query string
			var queryParts []string
			if len(queryParts) > 0 {
				path = path + "?" + strings.Join(queryParts, "&")
			}

			// Make request
			resp, err := ctx.Client.Do(reqCtx, "DELETE", path, nil)
			if err != nil {
				return err
			}
			defer resp.Body.Close()


			if resp.StatusCode == http.StatusNoContent {
				fmt.Fprintln(os.Stderr, "Deleted successfully")
				return nil
			}

			return ctx.Output.PrintResponse(resp)
		},
	}

	cmd.Flags().BoolVar(&flagYes, "yes", false, "Skip confirmation prompt")
	cmd.Flags().BoolVarP(&flagDryRun, "dry-run", "n", false, "Preview without executing")

	return cmd
}

func newAdvancedMobileDeviceSearchesDeleteMultipleCmd(ctx *CLIContext) *cobra.Command {
	var (
		flagYes bool
		flagDryRun bool
		flagIds []string
	)

	cmd := &cobra.Command{
		Use:   "delete-multiple",
		Short: "Remove specified Advanced Search objects",
		Long:  "Removes specified Advanced Search Objects",
		RunE: func(cmd *cobra.Command, args []string) error {
			reqCtx := context.Background()

			// Confirmation for destructive action
			if flagDryRun {
				fmt.Fprintf(os.Stderr, "Would delete-multiple\n")
				return nil
			}
			if !flagYes {
				noInput, _ := cmd.Flags().GetBool("no-input")
				if noInput {
					return fmt.Errorf("destructive operation requires --yes when --no-input is set")
				}
				fmt.Fprintf(os.Stderr, "⚠️  This will delete-multiple. Type 'yes' to confirm: ")
				var confirm string
				fmt.Scanln(&confirm)
				if confirm != "yes" {
					return fmt.Errorf("aborted")
				}
			}

			// Build request path
			path := "/v1/advanced-mobile-device-searches/delete-multiple"

			// Build query string
			var queryParts []string
			if len(queryParts) > 0 {
				path = path + "?" + strings.Join(queryParts, "&")
			}

			// Make request
			// Read body from stdin if available
			var body io.Reader
			// Handle --ids flag for bulk operations
			if len(flagIds) > 0 {
				idsJSON := fmt.Sprintf(`{"ids":[%s]}`, strings.Join(func() []string {
					quoted := make([]string, len(flagIds))
					for i, id := range flagIds {
						quoted[i] = fmt.Sprintf(`"%s"`, id)
					}
					return quoted
				}(), ","))
				body = strings.NewReader(idsJSON)
			} else {
				stat, _ := os.Stdin.Stat()
				if (stat.Mode() & os.ModeCharDevice) == 0 {
					body = os.Stdin
				}
			}
			resp, err := ctx.Client.Do(reqCtx, "POST", path, body)
			if err != nil {
				return err
			}
			defer resp.Body.Close()


			return ctx.Output.PrintResponse(resp)
		},
	}

	cmd.Flags().BoolVar(&flagYes, "yes", false, "Skip confirmation prompt")
	cmd.Flags().BoolVarP(&flagDryRun, "dry-run", "n", false, "Preview without executing")
	cmd.Flags().StringSliceVar(&flagIds, "ids", nil, "IDs to delete (comma-separated)")

	return cmd
}

