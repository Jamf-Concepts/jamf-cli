// Code generated by jamfpro-cli generator. DO NOT EDIT.
package generated

import (
	"context"
	"fmt"
	"io"
	"os"
	"strings"

	"github.com/spf13/cobra"
)

// NewOidcsCmd creates the oidcs command group
func NewOidcsCmd(ctx *CLIContext) *cobra.Command {
	cmd := &cobra.Command{
		Use:   "oidcs",
		Short: "Manage oidcs",
		Long:  `Manage oidcs in Jamf Pro.`,
	}

	cmd.AddCommand(newOidcsListCmd(ctx))
	cmd.AddCommand(newOidcsDispatchCmd(ctx))
	cmd.AddCommand(newOidcsGenerateCertificateCmd(ctx))

	return cmd
}

func newOidcsListCmd(ctx *CLIContext) *cobra.Command {
	var ()

	cmd := &cobra.Command{
		Use:   "list",
		Short: "Retrieve the URL to directly login to the IdP",
		Long:  "Retrieve the URL to directly login to the IdP",
		Example: `  # List all oidcs
  jamfpro-cli oidcs list

  # List oidcs and extract IDs
  jamfpro-cli oidcs list --field id`,
		RunE: func(cmd *cobra.Command, args []string) error {
			reqCtx := context.Background()

			// Build request path
			path := "/v1/oidc/direct-idp-login-url"

			// Build query string
			var queryParts []string
			if len(queryParts) > 0 {
				path = path + "?" + strings.Join(queryParts, "&")
			}

			// Make request
			resp, err := ctx.Client.Do(reqCtx, "GET", path, nil)
			if err != nil {
				return err
			}
			defer resp.Body.Close()

			return ctx.Output.PrintResponse(resp)
		},
	}

	return cmd
}

func newOidcsDispatchCmd(ctx *CLIContext) *cobra.Command {
	var (
		flagScaffold bool
	)

	cmd := &cobra.Command{
		Use:   "dispatch",
		Short: "Provide the url to redirect for OIDC login",
		Long:  "Provide the url to redirect for OIDC login based on email",
		RunE: func(cmd *cobra.Command, args []string) error {
			reqCtx := context.Background()

			if flagScaffold {
				fmt.Println(`{
  "emailAddress": "admin@domain.name",
  "originalUrl": "aHR0cHM6Ly9qYW1mLXByby11cmwuY29tL2xvZ2dpbmcuaHRtbA=="
}`)
				return nil
			}

			// Build request path
			path := "/v1/oidc/dispatch"

			// Build query string
			var queryParts []string
			if len(queryParts) > 0 {
				path = path + "?" + strings.Join(queryParts, "&")
			}

			// Make request
			// Read body from stdin if available
			var body io.Reader
			stat, _ := os.Stdin.Stat()
			if (stat.Mode() & os.ModeCharDevice) == 0 {
				body = os.Stdin
			}
			resp, err := ctx.Client.Do(reqCtx, "POST", path, body)
			if err != nil {
				return err
			}
			defer resp.Body.Close()

			return ctx.Output.PrintResponse(resp)
		},
	}

	cmd.Flags().BoolVar(&flagScaffold, "scaffold", false, "Print a JSON template for the request body and exit")

	return cmd
}

func newOidcsGenerateCertificateCmd(ctx *CLIContext) *cobra.Command {
	var ()

	cmd := &cobra.Command{
		Use:   "generate-certificate",
		Short: "Generate a new keystore used for signing OIDC messages",
		Long:  "Generates a new certificate used for signing OIDC messages",
		RunE: func(cmd *cobra.Command, args []string) error {
			reqCtx := context.Background()

			// Build request path
			path := "/v1/oidc/generate-certificate"

			// Build query string
			var queryParts []string
			if len(queryParts) > 0 {
				path = path + "?" + strings.Join(queryParts, "&")
			}

			// Make request
			// Read body from stdin if available
			var body io.Reader
			stat, _ := os.Stdin.Stat()
			if (stat.Mode() & os.ModeCharDevice) == 0 {
				body = os.Stdin
			}
			resp, err := ctx.Client.Do(reqCtx, "POST", path, body)
			if err != nil {
				return err
			}
			defer resp.Body.Close()

			return ctx.Output.PrintResponse(resp)
		},
	}

	return cmd
}
